import { VibrationDirective } from './vibration.directive';

describe('VibrationDirective', () => {
  it('should create an instance', () => {
    const directive = new VibrationDirective();
    expect(directive).toBeTruthy();
  });
});
